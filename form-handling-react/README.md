## 📝 Task 1 – Data Handling with React Query
- Implemented in `src/components/PostsComponent.jsx`.
- Wrapped with `QueryClientProvider` in `src/App.jsx`.
- Fetches posts from `https://jsonplaceholder.typicode.com/posts`.
- Includes loading, error, and refetch functionality.


# ALX Frontend React – Forms & Data Handling

## 📘 Project Overview
This project is part of the ALX Software Engineering React module.  
It covers:
- **Task 0:** Form handling in React (Controlled Components & Formik).
- **Task 1:** Data fetching and caching with React Query.

---

## 🚀 Setup Instructions
Clone the repository and install dependencies:

```bash
git clone https://github.com/<your-username>/alx-fe-reactjs.git
cd alx-fe-reactjs/form-handling-react
npm install
## 📝 Task 1 – Advanced Data Handling with React Query

**Objective:**  
Integrate React Query to fetch, cache, and update posts from an API.

**Implementation:**  
- Code is in `src/components/PostsComponent.jsx`.  
- Wrapped with `QueryClientProvider` in `src/App.jsx`.  
- Fetches posts from `https://jsonplaceholder.typicode.com/posts`.  
- Includes loading state, error handling, and a refetch button.

**How to Run:**  
```bash
npm install
npm run dev
