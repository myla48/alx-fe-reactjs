## 📝 Task 3 – Implementing and Testing a Todo List Component

**Objective:**  
Create a Todo List component in React and test it with Jest + React Testing Library.

**Implementation:**  
- Code in `src/components/TodoList.jsx`.  
- Integrated in `src/App.jsx`.  
- Features: add, toggle, delete todos.  

**Testing:**  
- Tests in `src/__tests__/TodoList.test.js`.  
- Covers rendering, adding, toggling, and deleting todos.  

**How to Run:**  
```bash
npm install
npm run dev   # run the app
npm test      # run the tests
